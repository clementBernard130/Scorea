{% extends '@EasyAdmin/crud/edit.html.twig' %}

{% block configured_head_contents %}
    {{ parent() }}
    <style>
        :root {
            --test-edit-bg-start: #f7f4ee;
            --test-edit-bg-end: #ecf6f4;
            --test-edit-primary: #0e8a7d;
            --test-edit-secondary: #0a5e60;
            --test-edit-accent: #ec6f2f;
            --test-edit-text: #15323a;
            --test-edit-muted: #5f7680;
            --test-edit-surface: #ffffff;
            --test-edit-border: #d9ebe7;
            --test-edit-shadow: rgba(12, 64, 67, 0.14);
        }

        .test-edit-page {
            position: relative;
            max-width: 1040px;
            margin: 0 auto;
            display: grid;
            gap: 1rem;
            padding-bottom: 0.75rem;
        }

        .test-edit-toolbar,
        .test-edit-hero,
        .test-edit-form-card {
            position: relative;
            z-index: 1;
        }

        .test-edit-toolbar {
            display: flex;
            align-items: center;
            justify-content: space-between;
            gap: 0.7rem;
            flex-wrap: wrap;
        }

        .test-edit-back {
            display: inline-flex;
            align-items: center;
            gap: 0.45rem;
            color: var(--test-edit-text);
            background: color-mix(in srgb, var(--test-edit-bg-end) 60%, #ffffff);
            border: 1px solid var(--test-edit-border);
            border-radius: 12px;
            padding: 0.55rem 0.9rem;
            text-decoration: none;
            font-weight: 600;
        }

        .test-edit-back:hover,
        .test-edit-back:focus,
        .test-edit-back:active,
        .test-edit-back:focus-visible {
            color: var(--test-edit-text) !important;
            background: #ffffff !important;
            border-color: color-mix(in srgb, var(--test-edit-primary) 32%, var(--test-edit-border)) !important;
        }

        .test-edit-pill {
            display: inline-flex;
            align-items: center;
            gap: 0.35rem;
            padding: 0.35rem 0.7rem;
            border-radius: 999px;
            background: color-mix(in srgb, var(--test-edit-accent) 20%, #ffffff);
            color: color-mix(in srgb, var(--test-edit-accent) 72%, #8a3f19);
            font-size: 0.79rem;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.06em;
        }

        .test-edit-hero {
            overflow: hidden;
            border-radius: 18px;
            border: 1px solid color-mix(in srgb, var(--test-edit-primary) 25%, var(--test-edit-border));
            background:
                linear-gradient(130deg, rgba(255, 255, 255, 0.88), rgba(255, 255, 255, 0.44)),
                linear-gradient(130deg, var(--test-edit-bg-start), var(--test-edit-bg-end));
            box-shadow: 0 20px 40px var(--test-edit-shadow);
            padding: 1.3rem;
        }

        .test-edit-title {
            margin: 0;
            font-size: clamp(1.3rem, 2.2vw, 1.8rem);
            line-height: 1.12;
            color: var(--test-edit-text);
            letter-spacing: -0.02em;
        }

        .test-edit-subtitle {
            margin: 0.55rem 0 0;
            color: var(--test-edit-muted);
            font-size: 0.95rem;
            max-width: 54ch;
        }

        .test-edit-form-card {
            border-radius: 18px;
            padding: 1.1rem;
            border: 1px solid color-mix(in srgb, var(--test-edit-primary) 18%, var(--test-edit-border));
            background: var(--test-edit-surface);
            box-shadow: 0 16px 32px rgba(14, 76, 84, 0.1);
        }

        .test-edit-section {
            border-radius: 14px;
            border: 1px solid color-mix(in srgb, var(--test-edit-primary) 16%, var(--test-edit-border));
            background: linear-gradient(180deg, #ffffff, color-mix(in srgb, var(--test-edit-bg-end) 22%, #ffffff));
            padding: 0.95rem;
        }

        .test-edit-section + .test-edit-section {
            margin-top: 0.85rem;
        }

        .test-edit-section-title {
            margin: 0 0 0.72rem;
            color: var(--test-edit-text);
            font-size: 0.94rem;
            font-weight: 700;
            display: flex;
            align-items: center;
            gap: 0.45rem;
            letter-spacing: 0.02em;
            text-transform: uppercase;
        }

        .test-edit-section-title::before {
            content: '';
            width: 11px;
            height: 11px;
            border-radius: 50%;
            background: linear-gradient(135deg, var(--test-edit-primary), var(--test-edit-accent));
            box-shadow: 0 0 0 4px color-mix(in srgb, var(--test-edit-primary) 22%, transparent);
        }

        .test-edit-grid {
            display: grid;
            grid-template-columns: repeat(2, minmax(0, 1fr));
            gap: 0.8rem;
        }

        .test-edit-form-card .form-label,
        .test-edit-form-card label {
            color: color-mix(in srgb, var(--test-edit-text) 88%, #102026);
            font-weight: 700;
            margin-bottom: 0.35rem;
        }

        .test-edit-form-card .form-control,
        .test-edit-form-card .form-select {
            border-radius: 10px;
            border: 1px solid color-mix(in srgb, var(--test-edit-primary) 18%, var(--test-edit-border));
            min-height: 42px;
            background: #ffffff;
        }

        .test-edit-form-card .form-control:focus,
        .test-edit-form-card .form-select:focus {
            border-color: color-mix(in srgb, var(--test-edit-primary) 70%, #0a5e60);
            box-shadow: 0 0 0 0.2rem color-mix(in srgb, var(--test-edit-primary) 22%, transparent);
        }

        .test-edit-form-card textarea.form-control {
            min-height: 110px;
        }

        .test-edit-form-card .ea-form-actions,
        .test-edit-form-card .form-actions {
            border-top: 1px dashed color-mix(in srgb, var(--test-edit-primary) 26%, var(--test-edit-border));
            margin-top: 0.9rem;
            padding-top: 0.9rem;
        }

        .btn-primary {
            border: none !important;
            color: #fff !important;
            border-radius: 11px !important;
            font-weight: 700;
            background: linear-gradient(135deg, var(--test-edit-primary), var(--test-edit-secondary)) !important;
            box-shadow: 0 10px 18px rgba(9, 94, 96, 0.24);
            padding: 0.55rem 1rem !important;
        }

        .btn-primary:hover,
        .btn-primary:focus,
        .btn-primary:active,
        .btn-primary:focus-visible {
            color: #fff !important;
            transform: translateY(-1px);
            box-shadow: 0 12px 20px rgba(9, 94, 96, 0.28);
        }

        @media (max-width: 780px) {
            .test-edit-grid {
                grid-template-columns: 1fr;
            }
        }
    </style>
{% endblock %}

{% block main %}
    <div class="test-edit-page">
        <div class="test-edit-toolbar">
            <a href="{{ ea_url().setAction('index') }}" class="test-edit-back">
                <i class="fa fa-arrow-left"></i> Retour
            </a>
            <span class="test-edit-pill"><i class="fa fa-pen"></i> Édition</span>
        </div>

        <section class="test-edit-hero">
            <h2 class="test-edit-title">Modifier le test</h2>
            <p class="test-edit-subtitle">Mettez à jour la matière, la section, l'enseignant, la date et le commentaire.</p>
        </section>

        <section class="test-edit-form-card">
            {{ form_start(edit_form) }}

            <div class="test-edit-section">
                <h3 class="test-edit-section-title">Informations du test</h3>
                <div class="test-edit-grid">
                    {% if edit_form.subject is defined %}
                        {{ form_row(edit_form.subject) }}
                    {% endif %}
                    {% if edit_form.section is defined %}
                        {{ form_row(edit_form.section) }}
                    {% endif %}
                </div>
                <div class="test-edit-grid">
                    {% if edit_form.teacher is defined %}
                        {{ form_row(edit_form.teacher) }}
                    {% endif %}
                    {% if edit_form.testDate is defined %}
                        {{ form_row(edit_form.testDate) }}
                    {% endif %}
                </div>
            </div>

            <div class="test-edit-section">
                <h3 class="test-edit-section-title">Commentaire</h3>
                {% if edit_form.comment is defined %}
                    {{ form_row(edit_form.comment) }}
                {% endif %}
            </div>

            {{ form_rest(edit_form) }}
            {{ form_end(edit_form, { render_rest: false }) }}
        </section>
    </div>
{% endblock %}
